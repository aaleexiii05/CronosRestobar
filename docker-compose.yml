services:
  mysql:
    image: mysql:8.3
    container_name: cronos-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 254123
      MYSQL_DATABASE: cronos_db
      TZ: America/Lima
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
